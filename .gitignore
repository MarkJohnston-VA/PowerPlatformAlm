# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# msbuild output directories
**/bin
**/obj

# MSBuild Binary and Structured Log
*.binlog

.vs

**/node_modules
packages

.env

# We don't want to save the binaries in the repo, since they'll 
#  have source files that will allow us to package them at build time.
*.msapp
*.dll
*.nupkg